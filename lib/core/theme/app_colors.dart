import 'package:flutter/material.dart';

class AppColors {
  // 🌙 Dark
  static const darkBackground = Color(0xFF121212);
  static const darkCard = Color(0xFF1E1E1E);
  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFF8E8E93);
  static const darkAccent = Color(0xFF7FE5D9);
  static const darkBorder = Color(0xFF2C2C2E);

  // ☀️ Light
  static const lightBackground = Color(0xFFF2F2F7);
  static const lightCard = Colors.white;
  static const lightTextPrimary = Color(0xFF1C1C1E);
  static const lightTextSecondary = Color(0xFF636366);
  static const lightAccent = Color(0xFF00A896);
  static const lightBorder = Color(0xFFE5E5EA);

  // 🎨 Habit accents (shared)
  static const green = Color(0xFF4CD964);
  static const blue = Color(0xFF5AC8FA);
  static const purple = Color(0xFF5856D6);
  static const red = Color(0xFFFF2D55);
  static const orange = Color(0xFFFF9500);
}